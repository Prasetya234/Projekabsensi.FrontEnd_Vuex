export default class User {
  constructor(username, email, password, nisn, nama, kelas, alamat) {
    this.username = username;
    this.email = email;
    this.password = password;
    this.nisn = nisn;
    this.nama = nama;
    this.kelas = kelas;
    this.alamat = alamat;
  }
}